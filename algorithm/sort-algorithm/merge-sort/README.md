# マージソート(Merge Sort)

分割統治法に基づくソートアルゴリズム。
元のデータをより扱いやすい小さな単位に分割し、それぞれをソートした後で統合(マージ)していく。
このアルゴリズムは安定な(予測可能な)ソートを提供し、大量のデータにも効率的に適用できる。

# 計算量

- 最悪計算量 : O(nlogn)
- 最良計算量 : O(nlogn)
- 平均計算量 : O(nlogn)
- 空間計算量 : O(n)